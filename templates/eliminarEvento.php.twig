<!DOCTYPE html>
<html lang="es">
<head>
    <meta charset="UTF-8">
    <title>Eliminar evento - Calendario escolar</title>
    <link rel="icon" type="image/png" href="../public/favicon.ico" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="../public/dist/output.css" rel="stylesheet">
    <link href="../public/css/fontawesome-free-6.5.0-web/css/all.css" rel="stylesheet">
</head>
<body class="bg-gray-100">
    <div class="text-center py-4 bg-gray-800 text-white">
        <h1 class="text-xl font-bold">Calendario escolar</h1>
        <div class="flex justify-center space-x-4 mt-2">
          <a href="../Evento/listarEventos" class="hover:text-gray-300">Volver atrás</a>
        </div>
    </div>
{% if loggedin %}
  <div class="container mx-auto px-4 mt-6">
    <h2 class="text-2xl font-bold mb-4">Eliminar evento</h2>
    <div class="bg-gray-200 p-4 rounded-md">
      <p class="mb-2"><i class="fas fa-exclamation-circle text-red-500"></i> ¿Estás seguro que deseas eliminar el evento?</p>
      <div class="p-4 bg-gray-100 rounded-md">
        {% set dias = ['Domingo', 'Lunes', 'Martes', 'Miércoles', 'Jueves', 'Viernes', 'Sábado'] %}
        {% set meses = ['enero', 'febrero', 'marzo', 'abril', 'mayo', 'junio', 'julio', 'agosto', 'septiembre', 'octubre', 'noviembre', 'diciembre'] %}
        <p><i class="fas fa-calendar-days"></i> Fecha: {{ dias[evento.fecha|date('w')] }}, {{ evento.fecha|date('j') }} de {{ meses[evento.fecha|date('n') - 1] }} de {{ evento.fecha|date('Y') }}</p>
        <p><i class="fas fa-graduation-cap"></i> Módulo profesional: {{ evento.nombre_asignatura }}</p>
        <p><i class="fas fa-book"></i> Evento: {{ evento.nombre }}</p>

      </div>
    </div>
    <form action="" method="POST" class="mt-4">
      <input type="hidden" name="id" value="{{ evento.id }}">
      <div class="flex items-center justify-between mt-4">
        <button class="px-4 py-2 bg-red-500 text-white rounded hover:bg-red-700" type="submit">Eliminar evento</button>
      </div>
    </form>
  </div>
{% else %}
<!-- Si no está logueado, se redigire al login -->
<script>
  window.location.href = "../Usuario/showLogin?error=2";
</script>
{% endif %}
</body>
</html>